/* Spreadshop embed configuration — must be loaded BEFORE shopclient */
window.spread_shop_config = {
  shopName: 'moe-bucks',
  prefix: 'https://moe-bucks.myspreadshop.com',
  baseId: 'shop',
  // Optional:
  // usePushState: false,
  // updateMetadata: true,
  // locale: 'en_US',
  // startToken: 'bestseller'
};
